<footer class="footer">
  <nav>
    <ul id="nav-bar">
      <ul id="nav-left-side">
        <li id="omni">
          <i id='omni-icon' class="fa fa-bullseye fa-5x"></i>
          <ul class="extra">
            <%= footer_button('sign-up','fa-arrow-circle-left') %>
            <%= footer_button('profile', 'fa-cog') %>
            <%= footer_button('help', 'fa-question-circle', link: help_path) %>
          </ul>
        </li>
        <!-- <div id="int-menu"> -->
          <% if params[:controller] == "glossaries" && params[:action] == "show" %>
              <%= render 'layouts/footer_terms' %>
          <% end %>
          <li id="debug_time"><%= Time.now.strftime('%H:%M:%S') %></li>
        <!-- </div> -->
      </ul>
      <%= footer_button('home', 'fa-home') %>
    </ul>
  </nav>
</footer>